from setuptools import setup

setup(name='biogeo',
      version='0.1',
      description='Calculate anthropogenic carbon and other common conservative and non conservative tracers',
      author='Sean Ridge',
      author_email='sridge@ldeo.columbia.edu',
      license='MIT',
      packages=['biogeo'],
      zip_safe=False)